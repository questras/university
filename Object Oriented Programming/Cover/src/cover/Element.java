package cover;

public interface Element {
    /**
     * Check if element contains number.
     *
     * @param number - number to check,
     * @return True if element contains number, false otherwise.
     */
    boolean contains(int number);
}
